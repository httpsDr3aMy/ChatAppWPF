<Window x:Class="ChatApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatApp"
        mc:Ignorable="d"
        Title="ChatApp" Height="450" Width="800"
        ResizeMode="NoResize"
        Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*" />
            <ColumnDefinition Width="0.9*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.85*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.RowSpan="2">
            <TextBlock Text="ChatApp" DockPanel.Dock="Top" TextAlignment="center" Margin="0 10" FontSize="18"/>
            <TextBox DockPanel.Dock="Top" Height="30"/>
            <Button DockPanel.Dock="Top" Height="30" Content="Connect"/>
            <ListView />
        </DockPanel>
        <StackPanel Grid.Column="1" Grid.Row="2">
            <StackPanel Orientation="Horizontal" >
                <TextBox Width="565" Height="45" VerticalContentAlignment="center"/>
                <Button Content="Send" Width="80" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
